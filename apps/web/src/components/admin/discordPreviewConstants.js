/** Discord client embed grid max width (px). */
export const DISCORD_EMBED_MAX_WIDTH = 432

/** Avatar + gap before message content (40px avatar + 16px gap). */
export const DISCORD_MESSAGE_CONTENT_OFFSET = 56

/** Total message row width for a typical embed message. */
export const DISCORD_MESSAGE_PREVIEW_WIDTH =
  DISCORD_EMBED_MAX_WIDTH + DISCORD_MESSAGE_CONTENT_OFFSET
